# Versiones puras de Python para evitar compiladores C++
fastapi>=0.100.0
# Usamos uvicorn sin [standard] para evitar uvloop y httptools que requieren C++
uvicorn>=0.22.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
reportlab>=4.0.0
python-multipart>=0.0.6

# Dependencias para generador de README
hypothesis>=6.0.0
markdown>=3.4.0
